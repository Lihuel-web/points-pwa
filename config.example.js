// Copia este archivo a `config.js` (que está ignorado) y completa tus credenciales de Supabase.
// No commitees `config.js` ni compartas la clave anon públicamente.
window.__SUPABASE_CONFIG = {
  url: 'https://YOUR-project-ref.supabase.co',
  anonKey: 'YOUR_SUPABASE_ANON_KEY'
};
